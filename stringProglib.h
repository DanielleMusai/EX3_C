int checkLetter(char c);// check the value of each char
int num(char *st);//a word numro
int isSign(char c);// check if its only letters
void Gematria(char *word, char *text);// A
int Atbash1(char a);// check the atbash for a single char
char *reverse(char *c);// a word reverse
char *atbashWord(char *word);//translate the word to atbash
void Atbash(char *word, char *text);//B
int charForC(char c);// check if there is a unnecessary sign
void Anagram(char *word, char *text);// C
